# zed-pkg-main

Supabase project reference: `zxjvldcyqobiupeiwjhx`

The GitHub integration working directory is `projects/zxjvldcyqobiupeiwjhx`. The nested `supabase/` directory is the provider deploy tree. This target is scaffolded as `planned`; do not enable production deployment until `target.json` passes the baseline and connection gates.

