# Migrations

This directory intentionally contains no deployment migration yet. Establish and review the hosted baseline with `supabase db pull`, then record its migration head and source commit in `../../target.json`. Do not copy a sibling RDS or legacy Supabase migration history without semantic reconciliation.

